# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

orbs:
  docker: circleci/docker@1.7.0

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
    coder-debian-base:
      jobs:
        - docker/publish:
            use-docker-credentials-store: false 
            docker-password: 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 
            docker-username: cfouche3005 
            docker-context: ./docker/debian-docker/base 
            dockerfile: coder-debian 
            image: coder-debian 
            path: ./docker/debian-docker/base 
            tag: $CIRCLE_SHA1,base 
            update-description: true 